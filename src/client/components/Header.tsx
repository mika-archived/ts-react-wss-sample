import React from "react";

import Container from "./controls/Container";

const Header: React.FC = () => {
  return (
    <Container>
      <h1>TypeScript + React Hooks + WebSocket Sample App</h1>
    </Container>
  );
};

export default Header;
